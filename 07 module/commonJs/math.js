const add = (num1, num2) => {
  return num1 + num2;
};

const multiply = (num1, num2) => {
  return num1 * num2;
};

module.exports = {
  add,
  multiply,
};

// Named Exports
// exports.add = add;
// exports.multiply = multiply;
